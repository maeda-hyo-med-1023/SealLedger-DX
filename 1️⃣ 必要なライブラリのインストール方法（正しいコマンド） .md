1️⃣ 必要なライブラリのインストール方法（正しいコマンド）
現在のエラー原因：python pip install のように python を先頭に付けているため、Pythonが pip というファイルを探してしまいエラーになっています。

正しいコマンドは以下です（ターミナルでそのまま実行してください）：

cmd
pip install pdf2image
pip install openpyxl
pip install pillow
pip install pytesseract
または、python -m を使う方法もあります：

cmd
python -m pip install pdf2image openpyxl pillow pytesseract
⚠️ 追加で必要なもの（Tesseract OCRエンジン本体）
pytesseract はPython用のラッパーライブラリで、実際のOCRエンジン（Tesseract OCR）が別途PCにインストールされている必要があります。

Tesseract OCR インストーラーをダウンロード
→ GitHub - tesseract-ocr/tesseract から Windows用インストーラー（例：tesseract-ocr-w64-setup-5.3.3.20231005.exe）をダウンロード。

インストール時に「日本語」言語データをチェックして追加インストールしてください。

インストール後、以下のようにパスを通すか、コード内でパスを指定します（後述コードで対応済み）。